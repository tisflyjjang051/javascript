const kor = 90;
const eng = 87;
const math = 75;
const sum = kor + eng + math;
const avg = sum / 3;
let score = "";
if (avg >= 90) {
  score = "A";
} else if (avg >= 80) {
  score = "B";
} else if (avg >= 70) {
  score = "c";
} else if (avg >= D0) {
  score = "D";
} else {
  score = "F";
}
console.log("🚀 ~ file: score.js:18 ~ score", score);
